alignment:
  method: sim3
  min_matches: 30
  ransac_threshold: 3.0
  max_iterations: 2000

pose_graph:
  robust_loss: huber
  huber_delta: 1.0
  max_iterations: 100

global_lm:
  enabled: true
  max_iterations: 50
  tolerance: 1.0e-6
